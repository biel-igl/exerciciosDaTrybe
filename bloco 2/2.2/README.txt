vamos ver git clone, que é ajudar um projeto que ja existe"
git add . adiciona todos os arquivos quer não foram commitados
git commit commita os arquivos, git commit -m adiciona uma frase que explica o que acontece no commit ( o -m tem essa função)
git log --diff-filter=D --summary .Explicação = git log(vou ver os commits que ja tive) --diff(diferença)-filter(filtrar)=D(arquivos deletados D de deletado) --summary(o jeito que aparare no caso sumario
git checkout(muda para onde eu quero voltar ou avansar o commit) ~1 (~1 passa a ser valido)
.gitignore arquivo que pode ser usado para ignorar arquivos para o git
git fetch ele procura no servidor se tem alguma coisa diferente da maquina local"

