vamos ver git clone, que é ajudar um projeto que ja existe"

git add . adiciona todos os arquivos quer não foram commitados

git commit commita os arquivos, git commit -m adiciona uma frase que explica o que acontece no commit ( o -m tem essa função)
